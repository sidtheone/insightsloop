# Cartographer Brief

## Changed Files
{{CHANGED_FILE_PATHS}}

## Diff
{{MERGED_DIFF}}

## Mission
Enumerate every code path in the diff. For each path, check: is there an explicit guard? Report only unguarded paths. Empty report (header only, no rows) is valid — do not invent findings.

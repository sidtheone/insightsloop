# Storm Brief (Verify Mode)

## Diff
{{MERGED_DIFF}}

## Findings Before Ship
{{FINDINGS_BEFORE_SHIP}}

## Mission
Run both passes in a single invocation — adversarial review first, consistency check second. For small changes (single worktree), write "Clean — single worktree, no cross-module changes." in the Consistency section. Separate introduced from pre-existing issues.
